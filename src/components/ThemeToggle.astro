---
---

<button
  id="theme-toggle"
  type="button"
  class="rounded-full border border-ctp-surface1 px-3 py-1.5 text-xs text-ctp-subtext0 hover:text-ctp-text hover:border-ctp-surface2 bg-ctp-mantle/50 transition-colors flex items-center gap-1.5 cursor-pointer"
  aria-label="Toggle Catppuccin Theme"
  title="Toggle Theme"
>
  <span id="theme-toggle-light-icon" class="hidden flex items-center gap-1.5">
    ☀️ <span class="font-mono text-xs">Latte</span>
  </span>
  <span id="theme-toggle-dark-icon" class="hidden flex items-center gap-1.5">
    🌙 <span class="font-mono text-xs">Macchiato</span>
  </span>
</button>

<script>
  function updateThemeUI() {
    const isLatte = document.documentElement.classList.contains('latte');
    const lightIcon = document.getElementById('theme-toggle-light-icon');
    const darkIcon = document.getElementById('theme-toggle-dark-icon');
    
    if (isLatte) {
      lightIcon?.classList.remove('hidden');
      darkIcon?.classList.add('hidden');
    } else {
      lightIcon?.classList.add('hidden');
      darkIcon?.classList.remove('hidden');
    }
  }

  function setupThemeToggle() {
    const button = document.getElementById('theme-toggle');
    updateThemeUI();

    button?.addEventListener('click', () => {
      const isLatte = document.documentElement.classList.contains('latte');
      if (isLatte) {
        document.documentElement.classList.remove('latte');
        document.documentElement.classList.add('dark', 'macchiato');
        localStorage.setItem('theme', 'dark');
      } else {
        document.documentElement.classList.remove('dark', 'macchiato');
        document.documentElement.classList.add('latte');
        localStorage.setItem('theme', 'latte');
      }
      updateThemeUI();
    });
  }

  setupThemeToggle();
  document.addEventListener('astro:after-swap', setupThemeToggle);
</script>
