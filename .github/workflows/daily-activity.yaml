name: Daily Org GitHub Activity Collector

on:
  schedule:
    # Runs at 00:30 UTC daily (06:00 IST)
    - cron: '30 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect-activity:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Collect GitHub Activity
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN || secrets.GITHUB_TOKEN }}
          GITHUB_ORG: ${{ vars.GITHUB_ORG || 'pid7-org' }}
        run: npm run github:activity -- --incremental --days 7

      - name: Commit and push updated activity JSON
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add src/assets/gh-activity.json

          if git diff --cached --quiet; then
            echo "No activity updates to commit."
          else
            git commit -m "gh-activity: update org github activity dataset [skip ci]"
            git push
          fi
